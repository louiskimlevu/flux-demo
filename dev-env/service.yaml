apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: dev
  labels:
    app: hello
spec:
  selector:
    app: MyApp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080